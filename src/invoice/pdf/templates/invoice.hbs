<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Tax Invoice</title>
    <style>
      @page {
        size: A4;
        margin: 10mm;
      }
      * {
        box-sizing: border-box;
      }
      body {
        font-family: "Arial", sans-serif;
        font-size: 9pt;
        color: #000;
        line-height: 1.3;
      }
      .container {
        width: 100%;
        border: 2px solid #ccc; /* Outer border */
        padding: 10px;
        border-radius: 5px;
      }
      .main-header {
        text-align: center;
        font-size: 14pt;
        font-weight: bold;
        margin-bottom: 20px;
        text-transform: capitalize;
      }

      /* Top Header Grid */
      .header-grid {
        display: flex;
        justify-content: space-between;
        margin-bottom: 20px;
      }
      .header-left,
      .header-right {
        width: 35%;
      }
      .header-center {
        width: 30%;
        text-align: center;
      }

      .header-field {
        margin-bottom: 5px;
      }
      .header-label-en {
        font-weight: bold;
        display: block;
        font-size: 8pt;
      }
      .header-label-ar {
        font-weight: bold;
        display: block;
        text-align: right;
        font-size: 8pt;
        direction: rtl;
      }
      .header-value {
        font-weight: bold;
        font-size: 9pt;
      }
      .header-value.rtl {
        text-align: right;
      }

      .qr-code {
        width: 120px;
        height: 120px;
      }

      /* Section Styling */
      .section-container {
        border: 1px solid #e0e0e0;
        border-radius: 8px; /* Rounded corners for section */
        padding: 15px;
        margin-bottom: 15px;
      }
      .section-header-title {
        display: flex;
        justify-content: space-between;
        font-weight: bold;
        font-size: 9pt;
        margin-bottom: 10px;
      }

      /* Form Row Layout */
      .row {
        display: flex;
        gap: 10px;
        margin-bottom: 8px;
      }
      .col {
        flex: 1;
      }

      .form-field {
        width: 100%;
      }

      /* Split label and value styling */
      .field-label-en {
        font-size: 7pt;
        color: #333;
        font-weight: bold;
        margin-bottom: 2px;
      }
      .field-label-ar {
        font-size: 7pt;
        color: #333;
        font-weight: bold;
        margin-bottom: 2px;
        text-align: right;
        direction: rtl;
      }
      .field-value-box-en {
        background-color: #f9f9f9;
        border: 1px solid #ddd;
        padding: 6px 8px;
        min-height: 25px;
        font-size: 8pt;
        border-radius: 3px;
      }
      .field-value-box-ar {
        background-color: #f9f9f9;
        border: 1px solid #ddd;
        padding: 6px 8px;
        min-height: 25px;
        font-size: 8pt;
        border-radius: 3px;
        text-align: right;
        direction: rtl;
      }

      /* Items Section Container (Specific to new request) */
      .items-section-container {
        border: 1px solid #ddd;
        border-radius: 8px;
        padding: 15px;
        margin-top: 20px;
      }

      .items-header-bar {
        display: flex;
        justify-content: space-between;
        font-weight: bold;
        font-size: 9pt;
        margin-bottom: 10px;
      }

      /* Table */
      .items-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 9pt;
      }
      .items-table th {
        padding: 10px;
        text-align: center;
        font-weight: bold;
        border-bottom: 1px solid #ddd;
        vertical-align: top;
        background-color: transparent; /* Updated to match image, seems white */
      }
      .items-table td {
        padding: 15px 10px;
        text-align: center;
        border-bottom: 1px solid #ddd;
        vertical-align: middle;
        font-weight: bold;
      }
      .items-table td.desc {
        text-align: center;
      }
      .items-table tr:last-child td {
        border-bottom: none;
      }

      /* Totals Block (New Design) */
      .totals-block {
        width: 100%; /* Image shows it taking full width or nice blocks */
        margin-top: 20px;
        /* Using a grid or flex column structure */
        display: flex;
        flex-direction: column;
        border: 1px solid #ddd;
        border-radius: 8px;
        overflow: hidden; /* For rounded corners on children */
      }
      .total-row {
        display: flex;
        border-bottom: 1px solid #ddd;
        min-height: 50px;
      }
      .total-row:last-child {
        border-bottom: none;
      }

      .total-label-cell {
        flex: 2; /* Takes more space */
        background-color: #fcfcfc;
        padding: 10px 15px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        font-weight: bold;
        font-size: 8pt;
        text-align: center; /* Image shows centered labels? No, split. */
      }
      .total-label-text {
        display: block;
        text-align: center;
        margin-bottom: 2px;
      }

      .total-value-cell {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        font-size: 10pt;
        border-left: 1px solid #ddd;
      }
    </style>
  </head>
  <body>

    <div class="main-header">
      Simplified Tax Invoice
      <p>فاتورة ضريبية مبسطة</p>
    </div>

    <div class="container">

      <!-- Top Header -->
      <div class="header-grid">
        <div class="header-left">
          <div class="header-field">
            <span class="header-label-en">Invoice reference number (IRN)</span>
            <span class="header-value">{{invoiceNumber}}</span>
          </div>
          <div class="header-field">
            <span class="header-label-en">Issue Date</span>
            <span class="header-value">{{issueDate}}</span>
          </div>
        </div>

        <div class="header-center">
          {{#if qrCode}}
            <img src="{{qrCode}}" class="qr-code" alt="QR Code" />
          {{/if}}
        </div>

        <div class="header-right">
          <div class="header-field">
            <span class="header-label-ar">الرقم التسلسلي</span>
            <div class="header-value rtl">{{invoiceNumber}}</div>
          </div>
          <div class="header-field">
            <span class="header-label-ar">تاريخ الإصدار</span>
            <div class="header-value rtl">{{issueDate}}</div>
          </div>
        </div>
      </div>

      <!-- Seller Section -->
      <div class="section-container">
        <div class="section-header-title">
          <span>Seller Identification</span>
          <span>هوية المورد</span>
        </div>

        <div class="row">
          <div class="col form-field">
            <div class="field-label-en">Seller Name</div>
            <div class="field-value-box-en">{{sellerName}}</div>
          </div>
          <div class="col form-field">
            <div class="field-label-ar">اسم المورد</div>
            <div class="field-value-box-ar">{{sellerNameAr}}</div>
          </div>
        </div>

        <div class="row">
          <div class="col form-field">
            <div class="field-label-en">VAT Registration Number</div>
            <div class="field-value-box-en">{{sellerVatNumber}}</div>
          </div>
          <div class="col form-field">
            <div class="field-label-ar">رقم تسجيل ضريبة القيمة المضافة</div>
            <div class="field-value-box-ar">{{sellerVatNumberAr}}</div>
          </div>
        </div>

        <div class="row">
          <div class="col form-field">
            <div class="field-label-en">Address</div>
            <div class="field-value-box-en">{{sellerAddress}}</div>
          </div>
          <div class="col form-field">
            <div class="field-label-ar">العنوان</div>
            <div class="field-value-box-ar">{{sellerAddressAr}}</div>
          </div>
        </div>
      </div>

      <!-- Buyer Section (Optional) -->
      {{#if buyerName}}
        <div class="section-container">
          <div class="section-header-title">
            <span>Buyer Identification</span>
            <span>هوية العميل</span>
          </div>

          <div class="row">
            <div class="col form-field">
              <div class="field-label-en">Buyer Name</div>
              <div class="field-value-box-en">{{buyerName}}</div>
            </div>
            <div class="col form-field">
              <div class="field-label-ar">اسم العميل</div>
              <div class="field-value-box-ar">{{buyerNameAr}}</div>
            </div>
          </div>
          {{#if buyerVatNumber}}
            <div class="row">
              <div class="col form-field">
                <div class="field-label-en">VAT Registration Number</div>
                <div class="field-value-box-en">{{buyerVatNumber}}</div>
              </div>
              <div class="col form-field">
                <div class="field-label-ar">رقم تسجيل ضريبة القيمة المضافة</div>
                <div class="field-value-box-ar">{{buyerVatNumberAr}}</div>
              </div>
            </div>
          {{/if}}
          <div class="row">
            <div class="col form-field">
              <div class="field-label-en">Address</div>
              <div class="field-value-box-en">{{buyerAddress}}</div>
            </div>
            <div class="col form-field">
              <div class="field-label-ar">العنوان</div>
              <div class="field-value-box-ar">{{buyerAddressAr}}</div>
            </div>
          </div>
        </div>
      {{/if}}

      <!-- Line Items Section (Round border container) -->
      <div class="items-section-container">
        <div class="items-header-bar">
          <span>Line Items</span>
          <span>معلومات وبيانات السلعة أو الخدمة</span>
        </div>

        <table class="items-table">
          <thead>
            <tr>
              <th width="35%">وصف السلعة أو الخدمة
                <br />
                Product or Service Description</th>
              <th width="15%">الكمية <br /> Quantity</th>
              <th width="20%">سعر الوحدة <br /> Unit Price</th>
              <th width="30%">إجمالي المبلغ شامل ضريبة القيمة المضافة
                <br />
                Subtotal Inclusive of VAT</th>
            </tr>
          </thead>
          <tbody>
            {{#each items}}
              <tr>
                <td class="desc">
                  <div
                    style="font-weight:bold; margin-bottom:5px;"
                  >{{description}}</div>
                  <!-- Ar name if available -->
                  <div style="font-weight:bold;">{{description}}</div>
                </td>
                <td>{{quantity}}</td>
                <td>{{unitPrice}} SAR</td>
                <td>{{totalAmount}} SAR</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>

      <!-- Totals Block (Matching Image) -->
      <!-- Image shows the totals block is separate, likely taking up some width or full width. Assuming full for simplicity or left aligned 60%? Image 2 shows it inside the same outer container? It looks like a separate table below items. -->

      <div class="totals-block">
        <!-- Taxable Amount -->
        <div class="total-row">
          <div class="total-label-cell">
            <span class="total-label-text" style="direction:rtl;">المبلغ الخاضع
              للضريبة (غير شامل ضريبة القيمة المضافة)</span>
            <span class="total-label-text">Invoice Taxable Amount</span>
          </div>
          <div class="total-value-cell">{{subTotal}} SAR</div>
        </div>

        <!-- VAT Amount -->
        <div class="total-row">
          <div class="total-label-cell">
            <span class="total-label-text" style="direction:rtl;">اجمالي مبلغ
              ضريبة القيمة المضافة</span>
            <span class="total-label-text">VAT Total Amount</span>
          </div>
          <div class="total-value-cell">{{vatAmount}} SAR</div>
        </div>

        <!-- Gross Total -->
        <div class="total-row">
          <div class="total-label-cell">
            <span class="total-label-text" style="direction:rtl;">إجمالي قيمة
              الفاتورة (شامل ضريبة القيمة المضافة)</span>
            <span class="total-label-text">Invoice Gross Total (Inclusive of
              VAT)</span>
          </div>
          <div class="total-value-cell">{{totalAmount}} SAR</div>
        </div>
      </div>

    </div>

  </body>
</html>